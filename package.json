{
  "name": "fabai",
  "private": true,
  "scripts": {
    "build": "cd react-app && npm install && npm run build",
    "start": "cd react-app && npm run start",
    "dev:web": "cd react-app && npm run dev"
  }
}
